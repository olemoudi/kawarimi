# Built binary
/kawarimi
/kawarimi-*

# Release / package artifacts
/dist/
*.zip

# Test / coverage output
*.out
coverage.txt
coverage.html

# Release signing private key (NEVER commit; lives as the RELEASE_SIGNING_KEY secret)
release-signing-key*
*.pem
*.key
